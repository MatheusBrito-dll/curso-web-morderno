/**
 * 01) Crie uma função que dado dois valores (passados como parâmetros) 
 * mostre no console a soma, subtração, multiplicação e divisão desses valores
 */
{
    {
        {
            {
                {
                    /**
                     * Funcao tradicional
                     * Funcao anonima
                     * Funcao anonima `Arrow` 
                     */
                }
            }
        }
    }
}

function soma(n1, n2) {
    return console.log(`${n1 + n2}`)
}
soma(2, 2)

const somaVar = function(n1, n2) {
    return console.log(`${n1 + n2}`)
}
somaVar(2, 2)

const somaVarArrow = (n1, n2) => console.log(`${n1 + n2}`)
somaVarArrow(2, 2)