const array = [];
const mensagem = array.length ? "O array não está vazio" : "O array está vazio";
console.log(mensagem); // "O array está vazio"


const isLoggedIn = true;
const mensagem2 = isLoggedIn ? "Bem-vindo de volta!" : "Por favor, faça o login";
console.log(mensagem); // "Bem-vindo de volta!"

const numero = -10;
const resultado = numero >= 0 ? "positivo" : "negativo";
console.log(resultado); // "negativo"
